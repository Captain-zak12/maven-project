trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- script: echo "Simulating Maven Build..."
  displayName: 'Maven Build (Simulated)'

- script: echo "Simulating Artifact Publish..."
  displayName: 'Publish Artifact (Simulated)'